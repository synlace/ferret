# [2026-05-14]

### Added
- feat(ui): `CreateFindingView` — styled finding card for `create_finding` tool calls; severity-coloured border/dot/badge (critical/high/medium/low/info), host line, description, copy-ID button
- feat(ui): `WriteTestView` — CM6 Python editor (left) + xterm.js output (right) for `write_test` / `write_pytest_file` tool calls
- feat(ui): `RunTestView` — command summary (left) + xterm.js terminal (right) for `run_test` / `run_pytest_file` tool calls
- feat(ui): `HttpRequestView` upgraded to CM6 wire-format panes (matching `get_request_detail`) — request and response rendered as HTTP/1.1 text in CodeMirror with content-type language detection

### Security
- fix(api): all router `except` blocks now call `deps.server_error(e)` instead of `HTTPException(status_code=500, detail=str(e))` — 58 occurrences across 7 router files; internal error details (DB messages, file paths, stack traces) no longer leak to the client; full exception is logged server-side via `logging.exception`
- fix(api): WebSocket `/ws` endpoint now checks the `Origin` header and closes with code 1008 (Policy Violation) for any origin that is not `localhost:{UI_PORT}` — prevents cross-origin pages from streaming proxy traffic via WS
- fix(api): unhandled exception handler now returns `{"detail": "Internal server error"}` instead of `str(exc)` — prevents stack traces, file paths, and DB error messages leaking to the client; real error is logged server-side via `logging.exception`
- fix(infra): Docker socket replaced with `tecnativa/docker-socket-proxy` — API container now connects via `DOCKER_HOST=tcp://docker-proxy:2375` with only `CONTAINERS`, `EXEC`, and `POST` operations permitted; image pull, volume mount, and privileged container creation are denied

### Docs
- docs(readme): restructured README — install instructions moved immediately below the title; added `UI_PORT` to configuration table; reduced from 145 to ~90 lines

### Security
- fix(api): restrict CORS `allow_origins` from wildcard to `http://localhost:{UI_PORT}` — prevents cross-origin reads of proxy traffic and stored keys from arbitrary pages
- fix(api): add `?confirm=destroy` guard to `DELETE /api/projects/reset` and `DELETE /api/projects/all` — prevents CSRF-triggered database wipes
- fix(api): resolve path before reading/writing in `GET /api/tests/files/{filename}` and `PUT /api/tests/files/{filename}` — closes path traversal that allowed `../ferret.db` overwrites

### Added
- feat(config): `UI_PORT` env var controls the host port the UI is exposed on (default `3000`); API CORS allowlist automatically matches; documented in `.env.example`

### Added
- feat(snare): full Snare module — two-phase HTTP intercept (request hold → forward → response hold → forward to client)
- feat(snare): client-disconnect detection — backend broadcasts `snare_client_disconnected` via WS when curl/browser closes connection while request is held; UI removes item and shows yellow toast instead of hanging on "Waiting for server response"
- feat(snare): resizable intercepted-requests sidebar with `localStorage` persistence; left action bar (Forward, Drop, Copy URL, Copy as cURL); REQ/RESP phase badges; editable CodeMirror editors for both phases
- feat(snare): drop kills TCP connection via injected 502 (`Response.make`) — request never reaches target
- feat(snare): snare rules — filter by host/path/method/body pattern; first matching rule wins; catch-all when no rules configured
- feat(ui): ferret mascot as favicon and sidebar top-left logo (collapsed: icon only; expanded: icon + text)

### Changed
- refactor(projects): tidy up projects page — consolidated three header bars into one, replaced inline editing with modal/sheet pattern
- refactor(projects): `KeysSheet` now slides up/down with CSS `translate-y` transition (always mounted in DOM, never conditionally rendered)
- refactor(projects): pre-fetch API keys for all projects on page load so sheet opens instantly with data already available
- fix(projects): spend always reported `$0` — switched from `GET /api/v1/keys/{hash}` with provisioning key to `GET /api/v1/key` authenticated with each sub-key's own bearer token; unwrap `{"data": {...}}` response envelope
- fix(projects): add `get_project_api_keys_with_values` DB method so spend/keys endpoints can access `key_value` without exposing it in public list responses
- fix(repeater): wrap `RepeaterPage` in `<Suspense>` to satisfy Next.js `useSearchParams()` boundary requirement (fixes Docker build failure)

- refactor(projects): `NewProjectModal` migrated from right-side slide-in panel to centred modal matching `EditProjectModal` style
- refactor(history): `DetailPanel` request/response panes replaced with CodeMirror 6 editors (atomone theme, HTTP/JSON/HTML/XML/CSS language detection, read-only) — matches repeater

### Added
- feat(findings): average cost per finding stat in the findings stats bar — shows `$X.XXXX` (or scientific notation for sub-cent values) derived from project spend ÷ total findings count
