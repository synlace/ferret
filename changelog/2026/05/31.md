# [2026-05-31]

### Added
- feat(justfile): Update `just shell` to support dropping into the `api` container shell as well as `runner` sandbox container.
- feat(rtk): Install `rtk` binary on the `api` container image and configure `just test api` to run the test suite via the `rtk` proxy. Add unit test to verify rtk availability.
- feat(justfile): Add log clearing functionality via `just logs clear` and `just logs clear <service>` supporting live truncation.

### Changed
- feat(settings): Prevent backup restores from auto-completing first-run setup wizard and triggering Fargate warm runner spawner. Restoring from a backup now only pre-fills the wizard steps, preserving the incomplete setup state until verified.
