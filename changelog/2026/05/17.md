# [2026-05-17]

### Security
- fix(infra): replace `tecnativa/docker-socket-proxy` with a custom `docker-shim` — allows only `POST /containers/ferret-lab/exec` (exec-create), `POST /exec/{id}/start|resize` (exec-start/resize), `PUT|GET|HEAD /containers/ferret-lab/archive` (docker cp), and `GET|HEAD /_ping` + `GET /containers/ferret-lab/json` (CLI negotiation); blocks `POST /containers/create`, `GET /containers/json`, and all other Docker API endpoints; restores test runner (`docker exec ferret-lab pytest`) and AI script runner (`docker cp` + `docker exec`) broken by the `CONTAINERS: 0` fix while keeping privileged container creation blocked; pins `docker-ce-cli=5:28.*` in the API image to match host daemon API version
